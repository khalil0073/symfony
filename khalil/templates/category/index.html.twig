{% extends 'base.html.twig' %}

{% block title %}Category indice{% endblock %}

{% block body %}
    <h1>Category indice</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Label</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.label }}</td>
                <td>
                    <a href="{{ path('category_show', {'id': category.id}) }}">Afficher</a>
                    <a href="{{ path('category_edit', {'id': category.id}) }}">editer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('category_new') }}">Créer un nouveau</a>
{% endblock %}
