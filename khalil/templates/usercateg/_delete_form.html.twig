<form method="post" action="{{ path('usercateg_delete', {'id': usercateg.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ usercateg.id) }}">
    <button class="btn">effacer</button>
</form>
