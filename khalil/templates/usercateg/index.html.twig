{% extends 'base.html.twig' %}

{% block title %}Usercateg indice{% endblock %}

{% block body %}
    <h1>Usercateg indice</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Label</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for usercateg in usercategs %}
            <tr>
                <td>{{ usercateg.id }}</td>
                <td>{{ usercateg.label }}</td>
                <td>
                    <a href="{{ path('usercateg_show', {'id': usercateg.id}) }}">Afficher</a>
                    <a href="{{ path('usercateg_edit', {'id': usercateg.id}) }}">editez</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2.8">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('usercateg_new') }}">Créer un nouveau</a>
{% endblock %}
